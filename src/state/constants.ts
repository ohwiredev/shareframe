/** Fixed Open Graph export size — preview CSS may scale; export stays full-res. */
export const OG_WIDTH = 1200;
export const OG_HEIGHT = 630;
export const OG_RATIO = OG_WIDTH / OG_HEIGHT;

/** Controls inspector width budget (layout docs: detail panel ~340–420). */
export const CONTROLS_PANEL_WIDTH = 360;
export const EXPORT_PANEL_WIDTH = 256;
